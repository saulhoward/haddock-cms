A Script to dump all MySQL DBs

© Clear Line Web Design, 2007-02-05

The dump files are saved in the directory given with
a name like:

<DUMP_DIR>/<HOST>/<DB>/<TABLE>/<TIME_STAMP>.dump

The details for the connecting to the database
and where to dump the files are set in the
project config file.

Usage:
    --silent
    --help
